# Public browser configuration (not secrets)
NEXT_PUBLIC_ENV="develop"
NEXT_PUBLIC_APP_URL="http://localhost:3005"
NEXT_PUBLIC_TIMEZONE="America/Argentina/Tucuman"

# Local Supabase, or the public values of the authorized hosted project.
NEXT_PUBLIC_SUPABASE_URL="http://127.0.0.1:55321"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Local/server secrets. Leave empty here; never commit the filled .env.local.
SUPABASE_SERVICE_ROLE_KEY=""
# Generate with openssl rand -hex 32.
CRON_AUTH_SECRET=""
